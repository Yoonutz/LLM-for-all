# Architecture

> This file is maintained by Copilot and rewritten on every implementation step. It always reflects the current state of the project.

## Overview

**LLM-for-all** is a course-driven project building up an LLM-based application incrementally, lesson by lesson. It uses LangChain with OpenRouter to interact with LLMs. The workspace root **is** the project — all lessons add files directly here, blending into a single unified structure.

## Package Manager

- **uv** (by Astral) — replaces pip, venv, pyenv, and poetry
- Installed via `winget install --id=astral-sh.uv -e`
- All dependencies managed through `pyproject.toml` + `uv.lock` at the workspace root
- No bare `pip install` used anywhere in this project

## Project Structure

```
LLM-for-all/                         ← workspace root = project root
├── .github/
│   └── copilot-instructions.md      # Copilot session rules
├── .venv/                           # Virtual environment (gitignored)
├── .env                             # API key — NOT committed to git
├── .gitignore
├── .python-version                  # Python 3.12
├── architecture.md                  # This file
├── pyproject.toml                   # Project metadata and dependencies
├── uv.lock                          # Deterministic lockfile — committed to git
├── README.md
├── 01_human_message.py              # Lesson 2 — first LLM request via HumanMessage
├── 02_01_prompts.py                 # Lesson 3 — four levels of prompting (zero-shot → CoT)
├── 02_02_prompt_templates.py        # Lesson 3 — PromptTemplate, ChatPromptTemplate, FewShot
├── 02_03_prompts_advanced.py        # Lesson 3 — LCEL chain + intent classifier
├── 03_lcel.py                       # Lesson 5 — four LCEL levels + streaming
├── 04_output_parsers.py             # Lesson 6 — Pydantic structured output, with_structured_output
├── 05_rag_loaders.py                # Lesson 7 — multi-format document loading, chunking, Chroma indexing
├── 06_rag_query.py                  # Lesson 7 — RAG chain: retriever | prompt | LLM | parser
├── 05_docs/                         # Source documents for RAG
│   ├── notes/
│   │   ├── 01_rag_multimodal.txt    # RAG & multimodal vectorization notes
│   │   └── 02_graphdb_graphrag.txt  # GraphDB & GraphRAG notes
│   ├── references/
│   │   └── references.txt           # LangChain RAG doc URLs
│   ├── csv/
│   │   └── 01_rag_eval.csv          # Q&A evaluation dataset
│   ├── pdf/                         # Place PDF papers here (manual — see Lesson 7)
│   └── docx/                        # Place DOCX files here (manual — see Lesson 7)
├── 05_vectorstore/                  # Chroma persistent vector store (auto-generated by loaders)
├── 07_memory.py                     # Lesson 8 — Buffer, Window, Persistent memory with LangGraph
├── 07_chat_history.db               # SQLite persistent memory store (auto-generated by 07_memory.py)
├── 08_agents.py                     # Lesson 9 — @tool decorator, bind_tools(), manual ReAct loop, dialogue agent
├── 09_mcp_server.py                 # Lesson 10 — FastMCP server: calculator, weather, temperature converter
├── 10_mcp_client.py                 # Lesson 10 — MCP client: discovers tools, converts to LangChain, ReAct agent
├── 11_assistant.py                  # Lesson 11 — Mini Perplexity: DuckDuckGo search, validation, Chroma notes, CLI
├── notes/                           # Markdown research notes (auto-created by 11_assistant.py)
├── notes_db/                        # Chroma vectorstore for semantic note search (auto-created)
└── lessons/                         # gitignored — lesson text + per-lesson resource diffs
    ├── 01-installing-uv/
    │   └── installing-uv.md
    ├── 02-openrouter-first-request/
    │   ├── openrouter-first-request.md
    │   └── resources/
    │       ├── .python-version
    │       ├── pyproject.toml
    │       └── 01_human_message.py
    ├── 03-prompts-in-practice/
    │   ├── prompts-in-practice.md
    │   └── resources/
    │       ├── 02_01_prompts.py
    │       ├── 02_02_prompt_templates.py
    │       └── 02_03_prompts_advanced.py
    ├── 04-how-gpt-was-born/
    │   └── how-gpt-was-born.md         # Theory-only — no resources
    ├── 05-lcel-pipeline/
    │   ├── lcel-pipeline.md
    │   └── resources/
    │       └── 03_lcel.py
    └── 06-output-parsers/
        ├── output-parsers.md
        └── resources/
            ├── 04_output_parsers.py
            └── pyproject.toml
    ├── 07-rag/
    │   ├── rag.md
    │   └── resources/
    │       ├── 05_rag_loaders.py
    │       ├── 06_rag_query.py
    │       ├── pyproject.toml
    │       └── 05_docs/             # text/csv files only; PDFs and DOCX require manual placement
    ├── 08-memory/
    │   ├── memory.md
    │   └── resources/
    │       ├── 07_memory.py
    │       └── pyproject.toml
    ├── 09-tools-and-agents/
    │   ├── tools-and-agents.md
    │   └── resources/
    │       └── 08_agents.py
    └── 10-mcp/
        ├── mcp.md
        └── resources/
            ├── 09_mcp_server.py
            ├── 10_mcp_client.py
            └── pyproject.toml
    └── 11-final-project/
        ├── final-project.md
        └── resources/
            ├── 11_assistant.py
            └── pyproject.toml
```

Each `resources/` folder contains **only the files introduced or modified in that lesson**, mirroring the live project root path. Applying lesson resources in order reproduces the full project state at any point.

## Dependencies

| Package                       | Version  | Role                                                                |
| ----------------------------- | -------- | ------------------------------------------------------------------- |
| `langchain`                   | ≥ 1.3.0  | Core framework                                                      |
| `langchain-openai`            | ≥ 1.2.1  | OpenAI-compatible API client (used with OpenRouter)                 |
| `langchain-core`              | ≥ 1.4.0  | Prompts, runnables, message history, `trim_messages`                |
| `langchain-community`         | ≥ 0.4.1  | Community loaders (TextLoader, CSVLoader, WebBaseLoader…)           |
| `langchain-text-splitters`    | ≥ 1.1.2  | `RecursiveCharacterTextSplitter` for chunking                       |
| `langchain-chroma`            | ≥ 1.1.0  | LangChain ↔ Chroma vector store integration                         |
| `langchain-huggingface`       | ≥ 1.2.2  | `HuggingFaceEmbeddings` wrapper for sentence-transformers           |
| `langgraph`                   | ≥ 1.2.0  | Stateful LLM graph workflows; replaces `RunnableWithMessageHistory` |
| `langgraph-checkpoint-sqlite` | ≥ 3.1.0  | SQLite-backed persistent memory checkpointer                        |
| `fastmcp`                     | ≥ 3.2.4  | MCP server — `@mcp.tool()` decorator, stdio/SSE transports          |
| `mcp`                         | ≥ 1.27.1 | MCP client SDK — `ClientSession`, `StdioServerParameters`           |
| `httpx`                       | ≥ 0.28.0 | Async HTTP client (used by the weather tool and page fetching)      |
| `ddgs`                        | ≥ 9.14.2 | DuckDuckGo search client — no API key required                      |
| `sentence-transformers`       | ≥ 5.5.0  | Local free embeddings — model `all-MiniLM-L6-v2`                    |
| `chromadb`                    | ≥ 1.5.9  | Local persistent vector database                                    |
| `pypdf`                       | ≥ 6.11.0 | PDF document loading                                                |
| `docx2txt`                    | ≥ 0.9    | DOCX document loading                                               |
| `beautifulsoup4`              | ≥ 4.14.3 | HTML parsing required by `WebBaseLoader`                            |
| `tiktoken`                    | ≥ 0.12.0 | Token counting utility                                              |
| `python-dotenv`               | ≥ 1.2.2  | Load secrets from `.env`                                            |
| `pydantic`                    | ≥ 2.0    | Data validation and structured output schemas                       |

## LLM Access

- **OpenRouter** — unified gateway to GPT, Claude, Gemini, Llama, and others
- Compatible with the OpenAI API; uses `ChatOpenAI` with `base_url="https://openrouter.ai/api/v1"`
- API key stored in `.env` as `OPENROUTER_API_KEY`

## Embeddings

- **HuggingFaceEmbeddings** with `all-MiniLM-L6-v2` — free, local, used in lessons 7–10
- **OpenAIEmbeddings** via OpenRouter — `text-embedding-3-small`, used in lesson 11 for Chroma note indexing
- First HuggingFace run downloads the model (~90 MB) from HuggingFace Hub automatically
- `HF_TOKEN` in `.env` removes the unauthenticated-request warning and raises rate limits (free HF account)
- Used in both `05_rag_loaders.py` (indexing) and `06_rag_query.py` (retrieval)

## Lessons Completed

| Folder                         | Topic                                                                                                                                         |
| ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `01-installing-uv/`            | uv package manager — installation, commands, lockfiles                                                                                        |
| `02-openrouter-first-request/` | OpenRouter setup, first `HumanMessage` request, secure API key handling                                                                       |
| `03-prompts-in-practice/`      | Zero-shot → CoT, `PromptTemplate`, `ChatPromptTemplate`, LCEL `\|`, intent classifier                                                         |
| `04-how-gpt-was-born/`         | Transformer architecture, self-attention (QKV), GPT pre-training → RLHF, prompt engineering theory                                            |
| `05-lcel-pipeline/`            | `Runnable` protocol, LCEL `\|` operator, four levels: manual → pipe → dynamic → streaming                                                     |
| `06-output-parsers/`           | Pydantic v2, `PydanticOutputParser` (legacy), `with_structured_output` (modern), nested models                                                |
| `07-rag/`                      | RAG pipeline: multi-format loaders, chunking, Chroma vectorstore, LCEL RAG chain, GraphRAG theory                                             |
| `08-memory/`                   | LangGraph `StateGraph`, `MemorySaver` (Buffer + Window), `SqliteSaver` (Persistent), `trim_messages`                                          |
| `09-tools-and-agents/`         | `@tool` decorator, `bind_tools()`, `AIMessage.tool_calls`, `ToolMessage`, manual ReAct loop                                                   |
| `10-mcp/`                      | MCP architecture, `FastMCP` server, `ClientSession`, dynamic tool discovery, `StructuredTool`                                                 |
| `11-final-project/`            | Mini Perplexity: DuckDuckGo search, `FewShotChatMessagePromptTemplate`, structured output validation, Chroma notes, semantic search, fallback |

## Next

Course complete — module wrap-up.
