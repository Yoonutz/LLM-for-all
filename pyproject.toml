[project]
name = "llm-for-all"
version = "0.1.0"
description = "LLM engineering course project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.3.0",
    "langchain-openai>=1.2.1",
    "python-dotenv>=1.2.2",
]
